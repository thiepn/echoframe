export const HOSTILE_ECHO_DEFINITION = Object.freeze({
  spawnDelayMs: 1000,
  warningMs: 700,
  lifetimeRangeMs: Object.freeze([3500, 4500]),
  maximumCopies: 2,
  initialCopyCap: 1,
  minimumSpawnDistance: 260,
  offsetRange: Object.freeze([180, 320]),
  maximumFireEventsPerSecond: 5,
  fallbackPathRadius: 170,
});
