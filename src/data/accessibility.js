export const ACCESSIBILITY_DEFAULTS = Object.freeze({
  pauseOnFocusLoss: true,
  persistentPlayerLocator: false,
  largerTelegraphOutlines: false,
});

export const VISUAL_DEFAULTS = Object.freeze({
  screenShake: 0.7,
  reducedFlashes: false,
  reducedParticles: false,
  highContrast: false,
  damageNumbers: true,
  aimLine: true,
  hudOpacity: 0.9,
});
