export const ELITE_ENCOUNTER_RULES = Object.freeze({
  activeThreatCapExcludesEliteSurcharge: true,
  // PLAYTEST: abstract modifier surcharge influences composition target but not simultaneous body cap.
  requiredResonantSupportCount: 1,
  reserveReplicatingEnemySlots: 1,
  maximumMajorExecutionsPerWindow: 2,
  majorExecutionWindowMs: 300,
});
