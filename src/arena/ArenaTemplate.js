export function validateArenaTemplateShape(template){const reasons=[];if(!template?.id)reasons.push('arena-template-id-missing');if(!Array.isArray(template?.solids))reasons.push('arena-template-solids-missing');if(!Array.isArray(template?.enemySockets))reasons.push('arena-template-sockets-missing');if(!template?.playerSpawn)reasons.push('arena-template-player-spawn-missing');return Object.freeze({valid:reasons.length===0,reasons:Object.freeze(reasons)});}
