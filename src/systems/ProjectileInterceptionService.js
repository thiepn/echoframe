export class ProjectileInterceptionService{constructor({echoPlaybackSystem,enemyProjectileManager,onDestroyed=null,telemetry=null}={}){Object.assign(this,{echoPlaybackSystem,enemyProjectileManager,onDestroyed,telemetry});this.accumulator=0;}update(deltaMs){this.accumulator+=deltaMs;if(this.accumulator<32)return;this.accumulator=0;for(const echo of this.echoPlaybackSystem.pool.activeItems){if((echo.phantomShieldRemaining??0)<=0)continue;for(const projectile of[...this.enemyProjectileManager.pool.activeItems]){if(Math.hypot(projectile.x-echo.x,projectile.y-echo.y)>34)continue;echo.phantomShieldRemaining--;const id=projectile.activationId;this.enemyProjectileManager.deactivate(projectile,'phantom-shield');this.telemetry&&(this.telemetry.phantomInterceptions++);this.onDestroyed?.(id,'phantom-shield');break;}}}}
