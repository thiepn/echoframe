export class ArcRelayBehavior{constructor(){this.counters=new Map();}registerHit(sourceId='player',profile){if(!profile)return null;const value=(this.counters.get(sourceId)??0)+1;this.counters.set(sourceId,value%profile.hitCadence);return value%profile.hitCadence===0?Object.freeze({targetCount:profile.targetCount,scalar:profile.scalar,range:profile.range,triggerDepth:1}):null;}reset(){this.counters.clear();}}
