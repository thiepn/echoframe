import path from 'node:path';import { readFile,writeFile } from 'node:fs/promises';import { DOCS,packageMetadata,readJson,runtimeVersion,sourceManifest } from './phase12-utils.mjs';const pkg=await packageMetadata();const runtime=await runtimeVersion();const source=await sourceManifest();const ff=await readJson(path.join(DOCS,'PHASE12_BROWSER_FIREFOX_VALIDATION.json'));const cr=await readJson(path.join(DOCS,'PHASE12_BROWSER_CHROMIUM_VALIDATION.json'));const audit=await readJson(path.join(DOCS,'PHASE12_FINAL_RELEASE_AUDIT.json'));const status=`# Phase 12 Current State\n\n- Package/runtime: \`${pkg.version}\` / \`${runtime}\`\n- Tests: baseline 1299 plus Phase 12 release-closure tests\n- Source manifest: \`${source.digest}\`\n- Chromium: **${cr?.passed?'passed':'not passed'}**\n- Supported Firefox: **${ff?.status??'not run'}**\n- Firefox game code executed: **${ff?.gameCodeExecuted===true}**\n- Final verdict: **${audit?.verdict??'not generated'}**\n\nVersion 1.0 promotion remains blocked until supported real Firefox and all final-version gates pass.\n`;await writeFile(path.join(DOCS,'CURRENT_STATE.md'),status);await writeFile(path.join(DOCS,'PHASE12_DEFECT_REGISTER.md'),`# Phase 12 Defect Register\n\n## Product defects\n\n| Severity | Count | Notes |\n|---|---:|---|\n| Critical | 0 | None discovered |\n| High | 0 | None discovered |\n| Medium | 0 | None accepted |\n| Low | 0 | None recorded |\n\n## Release-environment blockers\n\n1. Current Firefox ${ff?.browser?.version??''} aborts before page creation because the Linux 4.4 kernel rejects its GS-base syscall. This is not counted as a product pass or product defect.\n2. No Git repository, workflow execution, or public Pages URL is available.\n\nThese blockers prevent final sign-off without being reclassified.\n`);const checks=[['Phase 11 source verified',true],['Phase 11 web archive verified',true],['Canonical documents unchanged',true],['All 1299 prior tests retained',true],['Real supported Firefox game execution',ff?.gameCodeExecuted===true],['Firefox complete matrix',ff?.passed===true],['Chromium final matrix',cr?.passed===true],['Cross-browser determinism',false],['Root/subpath both browsers',false],['GitHub Actions execution',false],['Public URL when required',false],['Package/runtime 1.0.0',pkg.version==='1.0.0'&&runtime==='1.0.0'],['Final 60-cycle lifecycle',false],['Firefox lifecycle subset',false],['Final 30-minute idle',false],['Final 30-minute active soak',false],['Final performance',false],['Final source archive',false],['Final web archive',false],['Final sign-off',audit?.passed===true]];await writeFile(path.join(DOCS,'PHASE12_RELEASE_CHECKLIST.md'),`# Phase 12 Release Checklist\n\n${checks.map(([label,ok])=>`- [${ok?'x':' '}] ${label}`).join('\n')}\n\nUnchecked items remain release gates.\n`);await writeFile(path.join(DOCS,'BROWSER_SUPPORT.md'),`# Browser Support\n\n## Certified\n\n- Chromium desktop Linux: Phase 12 production revalidation ${cr?.passed?'passed':'not passed'}.\n\n## Required before final Version 1.0\n\n- Current supported Firefox/Gecko: ${ff?.status??'not run'}. Game execution: ${ff?.gameCodeExecuted===true}.\n\n## Not claimed\n\n- Safari/WebKit\n- Mobile browsers\n- Touch controls\n- Gamepad controls\n\nThe product remains \`${pkg.version}\`.\n`);const vr=`# Validation Report\n\n## Phase 12\n\n- Source manifest: \`${source.digest}\`\n- Package/runtime: \`${pkg.version}\` / \`${runtime}\`\n- Chromium: ${cr?.passed?'passed':'not passed'}\n- Supported Firefox: ${ff?.status??'not run'}\n- Final sign-off: ${audit?.passed?'passed':'withheld'}\n\nPhase 10 and Phase 11 reports remain historical evidence. Final Version 1.0 lifecycle, idle, soak, performance, packaging, CI, and public-deployment evidence has not been generated because the Firefox pre-promotion gate remains blocked.\n`;await writeFile(path.join(DOCS,'VALIDATION_REPORT.md'),vr);const readme=await readFile(path.join(DOCS,'RELEASE_NOTES_v1.0.0.md'),'utf8').catch(()=> '# Version 1.0 Release Notes\n');if(!readme.includes('Phase 12'))await writeFile(path.join(DOCS,'RELEASE_NOTES_v1.0.0.md'),`${readme.trim()}\n\n## Phase 12 status\n\nDraft only. Version 1.0 has not been promoted because supported Firefox certification is still blocked by the execution kernel.\n`);console.log(JSON.stringify({packageVersion:pkg.version,sourceManifestDigest:source.digest,firefoxStatus:ff?.status,verdict:audit?.verdict},null,2));
