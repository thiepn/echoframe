name: Phase 13 Materialize Source — Historical

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-status:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Completed source materialization workflow retained as historical evidence; no longer automatic."
